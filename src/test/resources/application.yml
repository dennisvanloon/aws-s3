cloud:
  aws:
    stack:
      auto: false
    region:
      static: eu-west-1
      auto: false
    instance:
      data:
        enabled: false

spring:
  main:
    allow-bean-definition-overriding: true

localstack:
  endpoint: http://localhost:4566

custom:
  bucket: s3demo
  sqs-queue-name: s3demo-sqs-queue